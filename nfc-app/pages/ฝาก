<!-- <button
        className="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"
        onClick={() => navigateToPageCard()}
      >
        Card
      </button>
      <button
        className="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"
        onClick={() => navigateToPageBoardGame() }
      >
        BoardGamePage
      </button>

      <button
        className="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"
        onClick={() => navigateToPageHome()}
      >
        Home
      </button>
      <button
        className="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"
        onClick={() => navigateToPageBoxBoardGame()}
      >
        BoxBoardGame
      </button>

      <button
        className="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"
        onClick={() => navigateToPageService()}
      >
        Service
      </button>

      <button
        className="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"
        onClick={() => navigateToPageFeedback()}
      >
        FeedBack
      </button> -->

      <!-- const navigateToPageCard = () => {
        router.push(`/Card/`); // Navigate to the Card page with the selected card name
      };
    
      const navigateToPageBoardGame = () => {
        router.push('/BoardGame'); // Navigate to the BoardGame page
      };
    
      const navigateToPageHome = () => {
        router.push('/Home'); // Navigate to the Home page
      };
    
      const navigateToPageBoxBoardGame = () => {
        router.push('/BoxBoardGame'); // Navigate to the BoxBoardGame page
      };
    
      const navigateToPageService = () => {
        router.push('/Service'); // Navigate to the Service page
      };
    
      const navigateToPageFeedback = () => {
        router.push('/Feedback'); // Navigate to the Service page
      }; -->